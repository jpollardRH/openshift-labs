apiVersion: v1
kind: PersistentVolume
metadata:
    name: pv29
spec:
  capacity:
    storage: 10Gi 
  accessModes:
  - ReadWriteMany
  nfs: 
    path: /srv/nfs/storage/pv29
    server: support1.861b.internal 
  persistentVolumeReclaimPolicy: Recycle 

